name: OrangeFox Build - Final v39

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    permissions:
      contents: write

    steps:
      - name: Checkout Workspace
        uses: actions/checkout@v4

      - name: Cleanup & Maximize Space
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc "/usr/local/share/boost" "$AGENT_TOOLSDIRECTORY"
          sudo docker image prune -a -f
          sudo apt-get clean

      - name: Set up Build Environment
        run: |
          sudo apt-get update
          sudo apt-get install -y bc build-essential zip curl libstdc++6 git wget python3 python2 gcc clang libssl-dev
          mkdir -p ~/bin
          curl https://storage.googleapis.com/git-repo-downloads/repo > ~/bin/repo
          chmod a+x ~/bin/repo
          sudo ln -sf ~/bin/repo /usr/bin/repo
          sudo ln -sf /usr/bin/python2 /usr/bin/python

      - name: Sync OrangeFox
        run: |
          mkdir -p /home/runner/fox
          git clone https://gitlab.com/OrangeFox/sync.git ~/orangefox_sync
          SYNC_SCRIPT=$(find ~/orangefox_sync -name "orangefox_sync.sh" | head -n 1)
          chmod +x "$SYNC_SCRIPT"
          cd $(dirname "$SYNC_SCRIPT")
          /bin/bash ./orangefox_sync.sh --branch 12.1 --path /home/runner/fox || true

      - name: Inject Tree & Start Build
        run: |
          # 1. Fetch Tree
          mkdir -p /home/runner/fox/device/samsung/gta9p
          cd /home/runner/fox/device/samsung/gta9p
          git init
          git remote add origin https://github.com/RobotsAreCrazy/custom_recovery_tree_samsung_gta9p.git
          git fetch origin twrp-12.1
          git reset --hard FETCH_HEAD
          
          # 2. Hard-Config (Don't let errors here stop us)
          cd /home/runner/fox
          rm -f device/samsung/gta9p/vendorsetup.sh || true
          
          # Ensure directories exist for the makefiles
          mkdir -p device/samsung/gta9p
          
          printf 'PRODUCT_MAKEFILES := $(LOCAL_DIR)/twrp_gta9p.mk\n' > device/samsung/gta9p/AndroidProducts.mk
          printf 'COMMON_LUNCH_CHOICES := twrp_gta9p-eng\n' >> device/samsung/gta9p/AndroidProducts.mk
          
          # 3. Environment & Build
          export ALLOW_MISSING_DEPENDENCIES=true
          export FOX_BUILD_DEVICE=gta9p
          export LC_ALL=C
          export FOX_USE_TWRP_DT=1
          export OF_MAINTAINER="RobotsAreCrazy"
          export SKIP_API_CHECK=true
          export FOX_VERSION="R12.1"
          
          # Use a subshell for the source/lunch/mka to keep env clean
          /bin/bash -c "source build/envsetup.sh && lunch twrp_gta9p-eng && mka recoveryimage -j$(nproc --all)"

      - name: Upload Artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: OrangeFox-A9P-Final
          path: |
            /home/runner/fox/out/target/product/gta9p/OrangeFox*.zip
            /home/runner/fox/out/target/product/gta9p/recovery.img
